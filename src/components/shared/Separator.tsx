import { View } from "react-native";

export default function Separator() {
  return (
    <View
      style={{
        width: "100%",
        height: 1,
        backgroundColor: "rgba(255,255,255,0.1)",
      }}
    />
  );
}
